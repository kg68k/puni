# ビープ音声ファイル

　Human68k は起動時に CONFIG.SYS の BEEP= で指定したファイルがあると、OS 内に
読み込んでビープ音声として設定する。ファイル名に制限はないが、標準では
BEEP.SYS が使われている。ADPCM データなので拡張子を .PCM とする場合も多い。

　ファイルはサンプリング周波数 15.6KHz で $ffff バイト以内の ADPCM データ。

==============================================================================
